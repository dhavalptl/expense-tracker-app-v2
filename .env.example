# Session HMAC secret for signed cookies (required outside local defaults)
SESSION_SECRET=replace-me-with-a-long-random-string

# Optional override for SQLite file (default: data/app.sqlite)
# DATABASE_PATH=data/app.sqlite
